const MesosMapper = {
  "framework_info.name": "name",
  "labels.labels": "labels",
  "agent_id.value": "slave_id",
  "framework_id.value": "framework_id",
  "agent_info.hostname": "hostname"
};

module.export = MesosMapper;
